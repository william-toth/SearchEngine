/*
 *William Toth
 *function headers for saving a webpage for TSE project
 *CS50, 21W
 */

/**********************pagesave********************/
/*Given a current page, id #, and directory name, will save that page's html
 *to a document called "id #" in the directory
 *returns nothing
 */
 void pagesave (webpage_t *currpage, int idnum, char *dirchar);
